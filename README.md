# hello-word-api-dot-net

API mínima em C# com .NET 9.

## Pré-requisitos

- .NET SDK 9 instalado (`dotnet --version`)

## Como executar

```bash
dotnet restore
dotnet run
```

## Endpoint

- `GET /` retorna `Hello World!`